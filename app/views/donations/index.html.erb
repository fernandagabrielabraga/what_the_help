<br>
<div class="container_donations d-flex">
  <div class="d-flex justify-content-around flex-wrap mx-2" >
    <% @donations.each do |donation| %>
      <div class="card d-flex flex-column align-items-center m-5">
        <div class="d-flex">
        <%= cl_image_tag donation.image.key, height: 300, width: 382, crop: :fill %>
        </div>
        <div class="card-body d-flex flex-column bg-white w-100">
          <h5 class="card-title"><%= donation.donation_name %></h5>
          <p class="card-text"><%= donation.description %></p>
          <%= link_to 'Donation', donation,class:"btn btn-primary" %>
        </div>
      </div>
        <!--<div class="card-trip col-3 m-4 bg-secondary d-flex flex-col align-items-center">
        <div>
          <%cl_image_tag donation.image.key, height: 300, width: 400, crop: :fill if donation.image.key%>
        </div>
        <br>
          <div class="card-donations">
            <h2><%= donation.donation_name %></h2>
          </div>
        <br>
        <div>
          <%= link_to 'Donation', donation,class:"btn btn-outline-primary rounded-4 ms-2 mb-2" %>
          <br>
        </div>
      </div>-->
    <% end %>
  </div>

  <div class="mapbox mx-4 d-flex justify-content-center" style="width: 40vw;">
    <div style="width: 100%; height: 600px;"
      data-controller="mapbox"
      data-mapbox-markers-value="<%= @markers.to_json %>"
      data-mapbox-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
    </div>
  </div>
  <br>
</div>
